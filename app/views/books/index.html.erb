<div class='container'>
    <p id="notice"><%= notice %></p>
    <div class='row'>
        <div class='col-xs-3'>
            <%= render 'users/left', book: @book, user: @user %>
        </div>
        <div class='col-xs-9'>
            <h2>Books</h2>
            <%= render 'books/list', books: @books %>
        </div>
    </div>
</div>
